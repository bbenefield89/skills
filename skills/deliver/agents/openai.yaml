interface:
  display_name: "Deliver"
  short_description: "Implement, review, and validate in one flow"
  default_prompt: "Use $deliver to implement this request through test-driven implementation, independent review, and final validation."
