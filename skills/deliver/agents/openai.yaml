interface:
  display_name: "Deliver"
  short_description: "Implement, clean, review, and validate in one flow"
  default_prompt: "Use $deliver to implement this specification through cleanup, review, and final validation."
